/** Bucket privado de comprobantes de transferencia (<user_id>/<archivo>). */
export const RECEIPTS_BUCKET = "receipts";

/** Cuánto vive el enlace firmado con el que el admin abre un comprobante. */
export const RECEIPT_LINK_SECONDS = 60 * 60;
